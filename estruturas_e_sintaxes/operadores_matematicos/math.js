      /**
       * Operadores Matematicos Em Javascript
       *  [+] , [-] ,[/] ,[*] ,[%]
       */
      
      var x = 10, y = 5; // declaracao de variaveis
        var resultado = x+y;
        console.log('Soma dos Valores: ' + resultado)
        var resultado = x-y;
        console.log('Subtração dos Valores: ' + resultado)
        var resultado = x*y;
        console.log('Multiplicação dos Valores: ' + resultado)
        var resultado = x/y;
        console.log('Divisão dos Valores: ' + resultado)

        // var x = 10.0, y = 5; // isso vai dar erro, pois são tipos diferentes.

        // x += 5; // Concatenação funciona igual as outras linguagens.

        // lembrando do fato de que n posso somar Variaveis(var) com tipagens diferentes
        // Ex : double x = 32.0 + int 5; // Vai dar erro.